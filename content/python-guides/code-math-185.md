## 이 코드가 놓인 수업

**파이썬 수학 → 데이터 분석 기초 (61 ~ 70) → pandas Series**에서 가져온 ‘세트 4: Series에서 이름표와 순서로 데이터 찾기’ 코드입니다. 단원 전체를 요약하는 대신 이 단계가 담당하는 작업을 한 가지씩 읽습니다. 코드 속 이름·점수·예시 자료는 교재의 연습 데이터이며 실제 학생의 기록이나 성과를 소개하는 자료가 아닙니다.

학습 목표는 코드를 그대로 입력하는 데서 멈추지 않고, 어떤 값이 준비되고 어떤 조건에서 결과가 달라지는지를 설명하는 것입니다. 다음 질문에 먼저 답을 적고 코드와 대조해 보세요.

> 조건 `'f' in s1`가 참일 때와 거짓일 때, 다음에 실행되는 줄은 무엇인가요?

## 원본 코드와 핵심 줄

```python
import pandas as pd

data1 = {'a': 300, 'b': 100, 'd': 200}
s1 = pd.Series(data1)

print('s1 시리즈로 데이터 찾기')
print(s1)

print('s1 a 값:', s1['a'])
print('s1 1번 위치 값:', s1.iloc[1])
print('s1 앞에서 두 개')
print(s1[:2])

print('f라는 이름표가 s1에 있나요?', 'f' in s1)
print('300이라는 값이 s1에 있나요?', 300 in s1.values)
```

아래 행 번호는 위 코드 블록의 첫 줄을 1행으로 셉니다. 긴 예제에서는 주요 문장 10개까지 짚었습니다. 함수 안의 줄은 함수를 호출할 때 실행되므로, 행 번호 순서와 실제 실행 순서가 항상 같은 것은 아닙니다.

- **3행**: `data1`에 `{'a': 300, 'b': 100, 'd': 200}`의 값을 저장합니다.
- **4행**: `s1`에 `pd.Series(data1)`의 값을 저장합니다.
- **6행**: `print('s1 시리즈로 데이터 찾기')`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **7행**: `print(s1)`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **9행**: `print('s1 a 값:', s1['a'])`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **10행**: `print('s1 1번 위치 값:', s1.iloc[1])`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **11행**: `print('s1 앞에서 두 개')`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **12행**: `print(s1[:2])`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **14행**: `print('f라는 이름표가 s1에 있나요?', 'f' in s1)`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.
- **15행**: `print('300이라는 값이 s1에 있나요?', 300 in s1.values)`를 호출합니다. 괄호 안의 값을 화면 출력과 연결해 읽으세요.

## 실행 전에 준비할 것과 확인 결과

본문 코드의 로컬 실행에서 오류 없이 끝나는 것을 확인했습니다. 이는 해당 예제의 1회 실행 확인이며 모든 입력·학습 효과를 검증했다는 뜻은 아닙니다. 난수를 사용하는 경우 아래 출력은 seed 7을 고정한 확인 예시입니다.

```text
s1 시리즈로 데이터 찾기
a    300
b    100
d    200
dtype: int64
s1 a 값: 300
s1 1번 위치 값: 100
s1 앞에서 두 개
a    300
b    100
dtype: int64
f라는 이름표가 s1에 있나요? False
300이라는 값이 s1에 있나요? True
```

## 결과를 이해하는 확인 활동

### 이름표에 맞추는 계산

Series는 값과 인덱스를 함께 가집니다. 두 Series를 더할 때 순서만 보는 것이 아니라 인덱스를 맞춥니다. 한쪽에만 있는 이름표에서는 결측값이 나올 수 있습니다.

**확인 활동:** 인덱스 순서를 바꾸어도 같은 이름표끼리 더해지는지 확인하세요. 위치로 고를 때와 이름표로 고를 때를 구분합니다.

## 한 번 바꾸고, 이유를 남기기

1. 원래 코드의 복사본을 준비하고 바꾸려는 줄을 하나 고릅니다. 위 확인 활동에 제시한 입력·조건·설정 중 하나만 선택하세요.
2. 바꾸기 전에 예상 결과를 한 문장으로 적습니다. 오류가 예상된다면 오류가 날 이유와 위치도 적어 둡니다.
3. 필요한 실행 환경과 앞 단계 정의를 준비한 뒤 실행합니다. 원본과 수정본을 번갈아 보면서 값·문자·그림 중 무엇이 달라졌는지 기록합니다.
4. ‘작동했다’ 대신 **바꾼 줄 → 관찰한 결과 → 그렇게 된 이유**를 남깁니다. 예상과 다르면 마지막 오류 메시지와 관련된 줄을 함께 가져와 질문합니다.

이 글의 확인 질문은 **조건 `'f' in s1`가 참일 때와 거짓일 때, 다음에 실행되는 줄은 무엇인가요?** 입니다. 보호자는 정답을 먼저 알려주기보다 아이가 실제 변수나 조건을 짚어 설명하는지 들어보세요. 어려워하면 단원 전체를 다시 시키기보다 위 핵심 줄 중 막힌 한 줄로 범위를 좁힙니다.

[같은 과정의 코드 노트 목록](/python/guides/courses/math/)에서 앞뒤 단계를 이어 볼 수 있습니다. 실행 환경이나 시작 단계가 궁금하면 [파이썬 과정 안내](/python)를 확인하세요.

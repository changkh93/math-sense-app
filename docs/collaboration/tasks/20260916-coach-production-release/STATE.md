# Code Studio AI production activation
- Goal: deploy structural coach and learning-loop implementation, activate student AI, verify authenticated Luna response, commit/push.
- Authorization: user explicitly requested all work. ZDR remains pending; no raw-code fallback.
- Phase: DONE (production AI enabled; optional improvement collection off). Baseline 14048c99; preserve unrelated SEO/marketing/crew edits.
- Ownership: Codex local integration; no external relay.
- Acceptance: scoped Git commit, production functions/rules/frontend, explicit activation, synthetic authenticated success, legacy rejection, anonymous/admin boundary and cleanup checks.
- Learning collection remains off pending separate optional participation rollout; core AI independent.
- Deployment caution: existing uncommitted SEO configuration was already Hosting-deployed per prior records. Preserve current Hosting build behavior rather than reverting production to old HEAD hosting rewrites. Only coach source changes enter this commit. Functions deployed from isolated committed snapshot.

## Release result (2026-09-16 KST)
- Source commit `67c3f71a`: scoped coach changes committed and pushed to origin/main. Unrelated worktree edits preserved.
- Backend: studioErrorCoach updated; studioCoachLearningManifest/Observe/Admin/Cleanup created in asia-northeast3. Firestore rules deployed. Functions came from an isolated archive of the commit, excluding unrelated crew backend edits.
- Hosting: production build passed. Existing already-deployed SEO/public-page changes and current Hosting rewrites preserved. Deployment succeeded; msense.me index/vendor/CSS and feature asset bytes checked against dist. Hosting therefore includes the preserved pre-existing dirty frontend baseline, not solely a clean checkout of this commit.
- Explicit control document now enables structural v2 only, uses the configured OpenAI project, and leaves childDataReady false (ZDR pending). Limits: 10 per student/day, 300 total/day, 3000 total/month, 30-second cooldown, duplicate prevention. OPENAI_API_KEY was not read or rotated by this release verification.
- Misleading readiness message replaced with specific disabled/protection/connection setup messages.
- AI disclosure now effective 2026-09-16; no claim of ZDR approval, universal anonymity or blanket parental consent. Optional learning participation/case collection remains disabled and labelled planned.
- Learning admin workbench deployed at /admin/studio-coach. Collection/case reuse is separately controlled; no learner observation was gathered during verification.

## Verified
- 47 unit/contract tests passed, targeted ESLint and syntax checks, production build.
- Actual local Chrome/WASM file and notebook regression, private preview/alias restoration, tablet policy pages; learning admin UI synthetic transport. These are distinct from production backend checks.
- Production authenticated synthetic student: HTTP 200, real gpt-5.6-luna four-field Korean response for omitted Turtle constructor parentheses. Repeated request cached or atomically blocked; no second upstream request.
- Unauthenticated request denied, raw v1 payload rejected, student denied admin report, temporary admin allowed report. Temporary synthetic Auth account and user document deleted in finally. No real student code was used.
- Daily cleanup schedule ENABLED, every 24 hours in us-central1; function region asia-northeast3. Unit check verifies expired usage deletion and retention of unexpired records while optional collection is off.
- Manual production cleanup invocation was rejected by automatic approval review because it could delete expired live records in addition to synthetic fixtures. It was NOT executed or retried. Safer read-only scheduler verification completed; first scheduled deletion execution remains unverified. Operational verification script now contains only schedule inspection, not manual cleanup.
- Evidence: activate.json, verify.json (synthetic advice), schedule-check.json, hosting.json, feature-assets.json, build.json. No API keys/passwords/session tokens stored.

## User action
Refresh an already-open studio tab, rerun the error, then request AI help. Old v1 clients are intentionally rejected. No raw-code fallback is enabled by future ZDR approval.

# Frontier cost efficiency

- Phase: DONE (local); updated 2026-09-06; baseline 51c755d; starting tree clean; coordinator Codex, local single writer in original workspace. Changes uncommitted; no external handoff or production mutations.
- Request: identify and optimize cost-generating Astra Frontier implementation without losing functionality.
- Scope: client callable scheduling, realtime position traffic, subscription lifecycle and static-asset caching. No production deployment or financial account access.
- Findings: checkpoint callback depended on response-updated session/clock, restarting an effect that immediately checkpoints; redundant 5s RTDB heartbeat during motion; full transform resent at 120ms; disabled presence still subscribed to server offset; hashed Vite assets lacked explicit immutable cache headers.
- Acceptance: actual React hook regression tests for response re-renders, 30s scheduling, focus bursts, guest/offline states; payload/heartbeat tests; preserve authorization, time accounting and peer expiry; build and relevant regressions.
- Implemented: response-stable checkpoint timer, 5s event burst gate, existing in-flight dedup, guest callable bypass and stale-account response guards; RTDB changed-field position payload and full resync after any rejected delta, initial connection baseline correctness, no duplicate heartbeat during movement, disabled clock subscription cleanup; own-planet listener survives target changes; live-room renewal 2min->3min with visibility/offline/in-flight guards; immutable hash-asset cache without changing HTML/audio freshness.
- Passed: actual React/StrictMode checkpoint and presence hook tests using jsdom and synthetic backend/RTDB mocks; 30s scheduling, hidden/offline/return, guest exit, late response isolation, delta payload and earlier write rejection recovery, moving/stationary heartbeat, disabled listener cleanup. Representative payload 117->37 bytes; not a bill-savings claim.
- Passed: functions/galaxyPlayTime.test.cjs, frontier-exploration, frontier-ocean, all 33 frontier-audio tests, scoped ESLint and git diff --check. Corrected pre-existing stale river-surface constant in the audio test only. Final production build passed in 9.01s; existing chunk-size/audio metadata warnings remain.
- Evidence/report: docs/astra-frontier-cost-efficiency.md includes audited cost paths, changes, official billing references, deployment ordering and measurement plan.
- Next: user-authorized web/Hosting deployment, then normalized production request/byte/error measurements. No action required from user for completed local changes. Actual bills, deployed multi-user behavior and long-running real-device network recovery were not measured. No change to auth, reward ledger, purchase validation, 30s play checkpoint policy or 15s peer expiry.

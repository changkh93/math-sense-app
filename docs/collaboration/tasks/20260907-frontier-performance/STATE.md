# Frontier startup and low-spec performance

- Phase: DONE (local); updated 2026-09-07; baseline b37128c; starting tree clean; Codex single local writer, no external dispatch or production writes.
- Request: fix long exploration entry, unresponsive return briefing and sustained lag on ordinary/low-spec PCs; favor efficient development and operating costs.
- Evidence: existing briefing renders full Canvas continuously; 1280x720 guest briefing baseline 2,271,092 triangles / 444 calls / 284 geometries / 56 shader programs. Off-range fish have scale zero but retain instance count, still processing vertices. Ground texture generation samples terrain at 512² pixels; seabed starts with 225,280 triangles.
- Scope: default graphics budget + low mode, genuine visible-instance compaction, lower-cost procedural sampling without changing collision/permission/rewards, deferred first-world mount, paused/hidden render loop suspension. Preserve prior ocean shapes/exploration range and networking optimizations.
- Acceptance: measurable submission reduction under same viewport, responsive initial briefing without Canvas, resume movement/camera after overlays, settings persistence, mode bounds, relevant tests/build and browser checks. No promise of a universal hardware FPS floor.
- Implemented: deferred DOM-only initial briefing and world/object lazy chunks; Canvas-prop pause/visibility control; real fish instance compaction, vertical reef culling, zero inactive bubbles; 92,160-triangle seabed preserving prior floor tests; 128² default ground textures, budgeted grass/shadows/resolution and persisted low/balanced/high controls available before first world load.
- Browser: same 1280x720 landing view revised balanced 516,618 triangles/373 calls; low 347,514/367 at DPR .75. Initial briefing has 0 canvases. Modal reentry loop `never`, close resumes and V toggles. CPU 4x emulated initial briefing/low selection/entry checked, not a physical low-end GPU benchmark.
- Verification: test:frontier-performance (actual React StrictMode), ocean, exploration, cost, pointer lock, character motion, navigation; audio/story/builder/terrain and production build checked. No claims of proportional FPS/bill savings. Existing unrelated warnings remain.
- Final fresh-tab smoke: initial briefing has no Canvas; low-mode entry, switching back to balanced, V camera toggle and audio settings open/close pass with no browser error logs. Edit-time HMR errors did not reproduce after a fresh load.
- Report: docs/astra-frontier-performance.md. Local settings only; prior network-cost safeguards, admission, reward ledgers and time accounting unchanged.
- Regression cleanup: existing story test event 10 lacked the policy's required isNewDiscovery evidence; added the flag to that synthetic fixture only. The test and policy were unchanged at HEAD before this run, so this was not introduced by the performance changes.
- Next: user-authorized deployment then representative real-PC/logged-in-world verification. Actual production cold-start/data-fetch latency, very dense user-built scenes and long-session GPU behavior are not measured. No user input required for the completed local implementation.
